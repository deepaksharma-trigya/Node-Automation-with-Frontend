'use strict';

/**
 * controller.js
 *
 * Main controller — delegates to automationController.
 * Add other feature controllers here as the project grows.
 */

const automationController = require('./controllers/automationController');

module.exports = automationController;